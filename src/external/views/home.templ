package views

import (
    "conso-tracker/src/external/views/layouts"
)

templ Home() {
    @layouts.Base() {
        <div class={ homeContainer() }>
            <div id="main-content" class="content is-centered">
                <p class="is-italic">Aucun fichier importé</p>
            </div>
        </div>
    }
}

css homeContainer() {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 90vh;
}
