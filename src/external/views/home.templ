package views

import (
    "conso-tracker/src/external/views/layouts"
)

templ Home() {
    @layouts.Base() {
        <div class={ homeContainer() }>
            <div class="content is-centered">
                <p style="font-style: italic">Aucun fichier importé</p>
            </div>
        </div>
    }
}

css homeContainer() {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
}
